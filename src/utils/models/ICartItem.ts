export type TypeCartItem = {
  product: {
    price: number;
    _id: string;
    product_name: string;
    images: string[];
  };
  quantity: number;
  _id: string;
  createdAt: string;
  updatedAt: string;
};
